# secrets -- never commit
.env
.env.*
!.env.example

__pycache__/
*.py[cod]
.pytest_cache/
eval-report.json
extracted/
*.zip

# stray artefacts, not part of this project
bash.exe.stackdump
ruvector.db
.claude/
.ruff_cache/

# supplied material, not ours to redistribute: the problem statement PDF and
# the synthetic dataset both ship with the brief, not with this repo.
*.pdf
base.pkl
